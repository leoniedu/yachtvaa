% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wind-current.R
\name{apparent_conditions}
\alias{apparent_conditions}
\title{Add apparent condition columns to segments with buoy data}
\usage{
apparent_conditions(segments, impute_missing_wind = FALSE)
}
\arguments{
\item{segments}{A data frame with segment + buoy columns.}

\item{impute_missing_wind}{If \code{TRUE}, treat missing or all-\code{NA} wind data
as zero (no wind) and emit a warning. Default \code{FALSE}.}
}
\value{
The input with additional columns:
\code{wind_angle_deg}, \code{wind_class}, \code{wind_component_kmh},
\code{current_angle_deg}, \code{current_class}, \code{current_component_kmh}.

Component signs: positive = assisting (reducing effort),
negative = opposing (increasing effort).
}
\description{
Takes a data frame of segments already matched with buoy data (via
\code{\link[=match_buoy_to_segments]{match_buoy_to_segments()}}) and adds relative wind/current angles,
classifications, and along-track components.
}
\details{
Required input columns:
\itemize{
\item \code{bearing_deg}: vessel heading from \code{\link[=fastest_straight_distance]{fastest_straight_distance()}}
\item \code{wind_direction_deg}: meteorological wind direction (FROM)
\item \code{wind_speed_kmh}: wind speed in km/h
\item \code{current_direction_deg}: oceanographic current direction (TO)
\item \code{current_speed_kmh}: current speed in km/h
}

When \code{impute_missing_wind = TRUE}, missing wind columns
(\code{wind_direction_deg}, \code{wind_speed_kmh}) or columns that are entirely
\code{NA} are replaced with zeros and a warning is issued. This allows the
pipeline to run when buoy wind data is unavailable.
}
