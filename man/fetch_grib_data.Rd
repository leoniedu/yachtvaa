% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grib-processing.R
\name{fetch_grib_data}
\alias{fetch_grib_data}
\title{Fetch and parse SISCORAR GRIB ocean current data}
\usage{
fetch_grib_data(date, area = "baiatos", resolution = 0.001)
}
\arguments{
\item{date}{Date or \code{"YYYY-MM-DD"} string.}

\item{area}{Character area name passed to \code{rsiscorar::get_grib()}.
Default \code{"baiatos"}.}

\item{resolution}{Numeric grid resolution. Default \code{0.001}.}
}
\value{
A list with elements:
\describe{
\item{u_layers}{SpatRaster with U-component layers (one per hour)}
\item{v_layers}{SpatRaster with V-component layers (one per hour)}
\item{grib_times}{POSIXct vector of hourly timestamps (UTC)}
\item{n_hours}{Integer number of hours}
}
}
\description{
Wraps \code{\link[rsiscorar:get_grib]{rsiscorar::get_grib()}} and splits the returned SpatRaster into
U (eastward) and V (northward) current velocity layers with hourly
timestamps.
}
