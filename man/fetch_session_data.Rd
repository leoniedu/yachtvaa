% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-acquisition.R
\name{fetch_session_data}
\alias{fetch_session_data}
\title{Fetch both paddler and buoy data for a session}
\usage{
fetch_session_data(
  session = NULL,
  date = Sys.Date(),
  athlete_ids = 1:70,
  start_time = "05:00",
  end_time = "09:00",
  boia_id = BUOY_ID_DEFAULT,
  buoy_pad_sec = BUOY_TIME_PAD_SEC,
  use_db = TRUE,
  overwrite_db = FALSE,
  cache_analysis = TRUE
)
}
\arguments{
\item{session}{A \code{treinus_session} object from \code{\link[rtreinus:treinus_auth]{rtreinus::treinus_auth()}}.}

\item{date}{A Date or character date (YYYY-MM-DD) to filter exercises.}

\item{athlete_ids}{Integer vector of athlete IDs to query. Default 1:70.}

\item{start_time}{Character HH:MM lower bound for session start.}

\item{end_time}{Character HH:MM upper bound for session start.}

\item{boia_id}{Integer buoy ID. Default 515.}

\item{buoy_pad_sec}{Seconds to pad before/after session for buoy fetch.
Default 3600 (1 hour).}

\item{use_db}{If \code{TRUE} (default), use rtreinus's local SQLite DB for
exercise lists.}

\item{overwrite_db}{If \code{TRUE}, overwrite existing DB entries with fresh API
data. Default \code{FALSE}.}

\item{cache_analysis}{If \code{TRUE} (default), use rtreinus's RDS cache for
exercise analysis (GPS records).}
}
\value{
A list with elements:
\itemize{
\item \code{records}: GPS records tibble
\item \code{buoy}: buoy data frame
}
}
\description{
Orchestrates \code{\link[=fetch_paddler_records]{fetch_paddler_records()}} and \code{\link[=fetch_buoy_data]{fetch_buoy_data()}} with
automatic time window padding for buoy data. Downstream packages handle
their own caching (rtreinus for exercises/analysis, simcostar for buoy).
}
